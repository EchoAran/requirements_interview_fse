# ROLE: Requirements Information Extraction & Slot Incremental Update Specialist.
# CONTEXT: A semi-structured interview is underway. You are updating, refining, or extending slots under the target topic.
# TASK: Based on the factual information provided by the interviewee in the latest conversation turn, perform accurate slot extraction, fact updates, detail refinements, or conflict identifications for the target topic. If the latest turn contains no new incremental information relevant to the target topic, make NO modifications and return an empty array [].

# INPUT:
1. Target Topic Description As [current_topic]:
   {current_topic_content}
2. Conversation History As [conversation_record] (focus especially on the latest interviewee response):
   {current_topic_conversation_record}
3. Target Topic Current Slot State As [current_topic_slots] (each item contains slot_number, slot_key, slot_value, state in ["empty", "filled", "uncertain", "conflict"], and is_necessary):
   {current_topic_info_slots}
4. Cross-Topic Known Information Digest As [other_topics_summary]:
   {entire_interview_info_slots}

# EXTRACTION & UPDATE STEPS:
## Step 1: Carefully examine the interviewee's latest response and extract clear business facts and requirements directly relevant to [current_topic].
## Step 2: Compare against the existing slots in [current_topic_slots] using their authoritative state:
  - For an existing empty slot (state="empty"), if the interviewee provides a definite confirmed fact, populate it with the extracted fact and set operation to "update".
  - If the interviewee provides a concrete candidate value but explicitly expresses that it is tentative, subject to confirmation, or might change (e.g., "Set for three days. Still awaiting confirmation from the legal department."), set operation to "mark_uncertain" with the concrete non-empty candidate value in slot_value.
  - If the interviewee explicitly chooses to defer, postpone, or keep a slot uncertain/tentative rather than providing further details or deciding right now (e.g., "暂记未确定", "先待定", "先记为待定", "目前只能暂定，先记为未确定", "keep as uncertain", "leave it tentative", "defer for now"), set operation to "defer_uncertain" with the current or tentative candidate value in slot_value.
  - If a slot is already in uncertain status (state="uncertain") and the interviewee now explicitly confirms or finalizes the value, set operation to "update" or "refine" with the confirmed value.
  - If the interviewee's latest statement directly contradicts a previously recorded value (in a state="filled" or state="uncertain" slot), set operation to "conflict", storing the latest statement in slot_value.
  - If the interviewee introduces a substantial new requirement attribute that belongs to this topic but is not covered by existing slots, set operation to "add" (provide slot_key, slot_value, and operation "add", "mark_uncertain", or "defer_uncertain").
## Step 3: Incremental Output: If the latest turn provides NO new facts for the target topic, return an empty array []. Do NOT return unchanged slots.

# STRICT RULES & GUARDRAILS:
1. No Data Erasure & No Hallucinated Uncertainty: Strictly prohibit outputting "None", "null", "N/A", "unknown", or empty strings "" to indicate uncertainty. If the interviewee has no idea / does not know and there is no candidate value, do NOT output or update that slot. Do NOT use "mark_uncertain" or "defer_uncertain" because of the model's own low confidence; it must be grounded in the interviewee's explicit tentative expression or explicit deferral choice.
2. Grounded Evidence Only: Base updates strictly on explicit statements in the latest turn. Do NOT hallucinate unstated rules or speculate.
3. Topic Boundary Discipline: Keep facts bounded to [current_topic]. Do not place requirements belonging to other topics into this topic's slots.
4. One Proposal Per Slot: Output each existing slot_number at most once. If multiple compatible facts from the latest turn belong to the same slot, combine them into one complete slot_value. Evaluate every operation against the original [current_topic_slots] state; output items do not update the state for later items.
5. Pure JSON Output: Output ONLY a valid JSON array matching the format below (or [] if no incremental updates exist).

# OUTPUT FORMAT (Direct pure JSON array; output [] if no incremental changes):
[
  {
    "slot_number": "slot-1-1-1",
    "slot_key": "User Prerequisite Eligibility",
    "slot_value": "Must complete real-name identity verification with no disciplinary violations",
    "operation": "update"
  },
  {
    "slot_number": "slot-1-1-2",
    "slot_key": "Approval Routing Workflow",
    "slot_value": "Standard requests are auto-approved; urgent requests require department head approval",
    "operation": "refine"
  },
  {
    "slot_number": "slot-1-1-3",
    "slot_key": "Audit Archive Retention Period",
    "slot_value": "3 days (tentative, awaiting legal team confirmation)",
    "operation": "mark_uncertain"
  }
]

